# Required — BuildTools tenant + credentials (push to Doppler buildtools-mcp/dev|prd)
BUILDTOOLS_TENANT=moss
BUILDTOOLS_USERNAME=
BUILDTOOLS_PASSWORD=

# Optional — set when running the HTTP/SSE transport (Phase 6)
HTTP_PORT=
HTTP_BEARER_TOKEN=
