node_modules/
dist/
*.log
.env
.env.local
.DS_Store

# Test coverage reports (auto-generated)
coverage/

# Local data dumps / scratch
omega_financial_statements.csv
*.csv

# Throwaway smoke/probe scripts created during dev
smoke-*.ts
smoke*.ts
probe-*.ts
run-*.ts

# A nested clone of this repo at the root was once committed as a gitlink and
# went stale; vitest discovered its duplicate tests and ran them against dead
# source. Anchored to the root so it only guards against that recurrence.
/buildtools-mcp/

# Ad-hoc replica probe scratch script — never checked in.
bt-probe.ts
